# check if the element is present in the aarya, if not return -1

# given arr is the array , n is the dize of the array, idx is the index number

def getByIndex(arr, n, idx):
    return arr[idx] if id <= len(arr) else -1
